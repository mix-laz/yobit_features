<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yobit.com.laz.yobit_features">

    <!-- io.flutter.app.FlutterApplication is an android.app.Application that
         calls FlutterMain.startInitialization(this); in its onCreate method.
         In most cases you can leave this as-is, but you if you want to provide
         additional functionality it is fine to subclass or reimplement
         FlutterApplication and put your custom class here. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    -
    <application
        android:name=".YobitApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="yobit_features">
        <service
            android:name=".YobitService"
            android:enabled="true"
            android:exported="true" />

        android:label="yobit_features"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <!-- This keeps the window background of the activity showing
                 until Flutter renders its first frame. It can be removed if
                 there is no splash screen (such as the default splash screen
                 defined in @style/LaunchTheme). -->
            <meta-data
                android:name="io.flutter.app.android.SplashScreenUntilFirstFrame"
                android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".YobitReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="yobit.com.laz.yobit_features.startservice" />
                <action android:name="yobit.com.laz.yobit_features.stopservice"/>
                <action android:name="yobit.com.laz.yobit_features.pairsprice"/>
            </intent-filter>

        </receiver>
    </application>
</manifest>
